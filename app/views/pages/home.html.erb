<% if user_signed_in? %>
	<h4>Medallion Number</h4>
	<%= form_tag cabs_path, :method => 'get' do %>
		<p>
			<%= text_field_tag :search %>
			<%= submit_tag "Go", :name => nil, :class => "btn" %>
		</p>
	<% end %>
<% else %>
	<h3>Please Sign In</h3>
<% end %>