<%= form_for(@comment) do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :comment %><br />
    <%= f.text_area :comment %>

    <%= f.label :cab_id %><br />
    <%= f.select :cab_id, Cab.all.map {|p| [p.medallion_number, p.id]} %>

    <%= f.label :check_in_id %><br />
    <%= f.select :check_in_id, CheckIn.all.map {|p| [p.id]} %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
